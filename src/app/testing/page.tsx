import TestEndpoint from "@/app/testing/TestEndpoint";

export default function Page() {
  return (
    <div className="flex h-screen place-items-center justify-center">
      <TestEndpoint />
    </div>
  );
}
